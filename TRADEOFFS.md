# TRADEOFFS

## SQLite Instead of PostgreSQL
SQLite was used for simplicity and faster setup, though PostgreSQL would be more scalable for production.

## Rule-Based Validation
Simple rule-based suspicious detection was implemented instead of machine learning models for faster delivery and easier explainability.

## No Authentication
Authentication was skipped to prioritize core workflow functionality within assignment timeline.